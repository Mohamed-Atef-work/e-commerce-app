enum RequestState {
  initial,
  loading,
  success,
  error,
}

enum AdminUser {
  admin,
  user,
}

enum PickedImageState {
  changed,
  noImage,
}

enum ImageState {
  noImage,
  network,
  local,
}
