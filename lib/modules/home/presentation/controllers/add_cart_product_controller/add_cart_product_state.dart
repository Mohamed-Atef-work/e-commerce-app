part of 'add_cart_product_cubit.dart';

/*class AddToCartProductState {
  final String? message;
  final RequestState addState;

  const AddToCartProductState({
    this.message,
    this.addState = RequestState.initial,
  });
  AddToCartProductState copyWith({
    String? message,
    RequestState? addState,
  }) =>
      AddToCartProductState(
        message: message ?? this.message,
        addState: addState ?? this.addState,
      );
}*/
